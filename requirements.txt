# Python 3.11 or newer (tabfm requires it)

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3
jupyter>=1.0

# Model 5
xgboost>=2.0

# Models 1 and 2. Pulls PyTorch, and downloads ~6.5 GB of weights on first use.
# On PowerShell the brackets need quoting:  pip install "tabfm[pytorch]"
tabfm[pytorch]>=1.0
